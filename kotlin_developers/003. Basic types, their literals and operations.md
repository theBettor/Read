### 1. Numbers

숫자 표현 방법 : 숫자를 표현하는데 사용되는 비트 수 → 가능한 숫자의 크기와 정밀도를 결정

- 소수점이 없는 정수(integer)
    - Int, Long, Byte, Short
- 소수점이 있는 부동 소수점(floating-point)
    - Float와 Double

소수점이 없는 단순한 숫자는 Int로 해석됩니다. 소수점이 있는 단순한 숫자는 Double로 해석됩니다.

```kotlin
fun main() {
	val numI = 42
	val numD = 3.14
	num // 이렇게하면 어떤게 Double이고 Int인지 알 수 있음.
```

숫자 뒤에 L 접미사를 붙여 Long을 생성할 수 있습니다. Long은 Int로 표현하기에 너무 큰 숫자 리터럴에도 사용됩니다.

```kotlin
fun main() {
	val numF1 = 42F
	val numF2 = 123.45F
	num // 둘다 Float임을 확인할 수 있음.
}
```

```kotlin
fun main() {
	val numF1 = 42F
	val numF2 = 123.45F
	num // 둘다 Float임을 확인할 수 있음.
}
```

바이트(Byte)나 쇼트(Short) 유형을 생성하는 접미사는 존재하지 않습니다. 

→ L, F 같은거 바이트나 쇼트는 없다는 뜻.

그러나 명시적으로 이러한 유형 중 하나로 형이 지정된 숫자는 해당 유형의 인스턴스를 생성합니다. 이는 롱(Long) 유형에도 동일하게 적용됩니다.

```kotlin
fun main() {
	val b: Byte = 123
	val s: Short = 345
	val l: Long = 345
}
```

이것은 변환이 아닙니다! Kotlin은 암시적 형 변환을 지원하지 않으므로, Int가 필요한 위치에서 Byte나 Long을 사용할 수 없습니다.

→ 아래에서 형변환 방법을 알려줌.

```kotlin
fun main() {
	val b: Byte = 123
	val l: Long = 123L
	
	val i1: Int = b
	val i2: Int = l // Type mismatch나고 당연히 Int를 필요로함. 
	// 이런식으로 conversion 할 수 없음.
}
```

한 숫자를 다른 유형으로 명시적으로 변환해야 하는 경우 toInt나 toLong과 같은 명시적 변환 함수를 사용합니다.

```kotlin
fun main() {
	val b: Byte = 123
	val l: Long = 123L
	val i: Int = 123
	
	val i1: Int = b.toInt()
	val i2: Int = l.toInt()
	val l1: Long = b.toLong()
	val l2: Long = i.toLong()
}
```

### 2. **Underscores in numbers : 가독성을 위함**

```kotlin
fun main() {
	val million = 1_000_000
	println(million) // 1000000
}
```

### 3. **Other numeral systems**

16진수 체계로 숫자를 정의하려면 0x로 시작하십시오. 2진수 체계로 숫자를 정의하려면 0b로 시작하십시오. 8진수 체계는 지원되지 않습니다.

```kotlin
fun main() {
	val hexBytes = 0xA4_D6_FE_FE
	println(hexBytes) // 2765553406
	val bytes = 0b01010010_01101101_11101000_10010010 
	println(bytes) // 1382934674
}
```

### 4. **Number and conversion functions**

```kotlin
fun main() {
	val i: Int = 123
	val b: Byte = 123
	val l: Long = 123L
	
	val n1: Number = i
	val n2: Number = b
	val n3: Number = l
}

abstract class Number {
	abstract fun toDouble(): Double 
	abstract fun toFloat(): Float 
	abstract fun toLong(): Long 
	abstract fun toInt(): Int 
	abstract fun toChar(): Char 
	abstract fun toShort(): Short 
	abstract fun toByte(): Byte
}

fun main() {
	val b: Byte = 123
	val l: Long = b.toLong()
	val f: Float = l.toFloat()
	val i: Int = f.toInt()
	val d: Double = i.toDouble()
	println(d) // 123.0
}
```

### **5. Operations on numbers**

```kotlin
fun main() {
	val i1 = 12
	val i2 = 34
	println(i1 + i2) // 46
	println(i1 - i2) // -22
	println(i1 * i2) // 408 
	println(i1 / i2) // 0
	
	val d1 = 1.4
	val d2 = 2.5
	println(d1 + d2) // 3.9
	println(d1 - d2) // -1.1
	println(d1 * d2) // 3.5
	println(d1 / d2) // 0.5599999999999999
}

fun main() {
	println(5 / 2) // 2, not 2.5
}

fun main() {
	println(5.toDouble() / 2) // 2.5
}

fun main() {
	println(1 % 3) // 1 
	println(2 % 3) // 2 
	println(3 % 3) // 0 
	println(4 % 3) // 1 
	println(5 % 3) // 2 
	println(6 % 3) // 0 
	println(7 % 3) // 1 
	println(0 % 3) // 0 
	println(-1 % 3) // -1 
	println(-2 % 3) // -2 
	println(-3 % 3) // 0
}

fun main() {
	var i = 1
  println(i) // 1
  i += 10
  println(i) // 11
  i -= 5
  println(i) // 6
  i *= 3
  println(i) // 18
  i /= 2
  println(i) // 9
  i %= 4
  println(i) // 1
  
  // Post-incrementation
  // increments value and returns the previous value
  println(i++) // 1
  println(i) // 2
  // Pre-incrementation
  // increments value and returns the new value
  println(++i) // 3
  println(i) // 3
  // Post-decrementation
  // decrements value and returns the previous value
  println(i--) // 3
  println(i) // 2
  // Pre-decrementation
  // decrements value and returns the new value
  println(--i) // 1
  println(i) // 1
}
```

### 6. **Operations on bits**

Kotlin은 또한 다음 메서드를 사용하여 비트 연산을 지원하며, 이 메서드들은 접미사 표기법(즉, 두 값 사이에)으로 호출할 수 있습니다:

and는 두 숫자에서 동일한 이진 위치에 1이 있는 비트만 유지합니다.
or는 한 숫자 또는 두 숫자 모두에서 동일한 이진 위치에 1이 있는 비트만 유지합니다.
xor는 두 숫자에서 동일한 이진 위치에 정확히 하나의 1이 있는 비트만 유지합니다.
shl은 왼쪽 값을 오른쪽에 지정된 비트 수만큼 왼쪽으로 시프트합니다.
shr은 왼쪽 값을 오른쪽에 지정된 비트 수만큼 오른쪽으로 시프트하며, 가장 왼쪽 비트는 부호 비트의 복사본으로 채웁니다.
ushr은 왼쪽 값을 오른쪽에 지정된 비트 수만큼 오른쪽으로 시프트하며, 가장 왼쪽 비트는 0으로 채웁니다.

```kotlin
 fun main() {
	 println(0b0101 and 0b0001) // 1, that is 0b0001 
	 println(0b0101 or 0b0001) // 5, that is 0b0101 
	 println(0b0101 xor 0b0001) // 4, that is 0b0100 
	 println(0b0101 shl 1) // 10, that is 0b1010 
	 println(0b0101 shr 1) // 2, that is 0b0010 
	 println(0b0101 ushr 1) // 2, that is 0b0010
 }
```

### 7. **BigDecimal and BigInteger**

Kotlin의 모든 기본 유형은 크기와 정밀도가 제한되어 있어 어떤 상황에서는 부정확하거나 잘못된 결과가 나올 수 있습니다.

```kotlin
fun main() {
	println(0.1 + 0.2) // 0.30000000000000004
	println(2147483647 + 1) // -2147483648
}
```

어쩔수 없다. 완벽한 정밀도와 무제한 숫자 크기가 필요한 경우, JVM에서는 무제한 크기 숫자를 사용하려면 소수 부분이 없는 숫자를 나타내는 BigInteger와 소수 부분이 있는 숫자를 나타내는 BigDecimal을 사용해야 한다. 둘 다 생성자, 팩토리함수(valueOf 등), 또는 숫자를 나타내는 기본 유형(toBigDecimal 및 toBigInteger 메서드)으로부터의 변환을 통해 생성할 수 있다.

```kotlin
import java.math.BigDeciaml
import java.math.BigInteger

fun main() {
	val i = 10
	val l = 10L
	val d = 10.0
	val f = 10.0F
	
	val bd1: BigDecimal = BigDecimal(123)
	val bd2: BigDecimal = BigDecimal("123.00") 
	val bd3: BigDecimal = i.toBigDecimal()
	val bd4: BigDecimal = l.toBigDecimal()
	val bd5: BigDecimal = d.toBigDecimal()
	val bd6: BigDecimal = f.toBigDecimal()
	val bi1: BigInteger = BigInteger.valueOf(123) 
	val bi2: BigInteger = BigInteger("123")
	val bi3: BigInteger = i.toBigInteger()
	val bi4: BigInteger = l.toBigInteger()
}

// BigDecimal과 BigInteger는 기본적인 수학 연산자도 지원합니다.

import java.math.BigDeciaml
import java.math.BigInteger

fun main() {
	val bd1 = BigDecimal("1.2") 
	val bd2 = BigDecimal("3.4") 
	println(bd1 + bd2) // 4.6 
	println(bd1 - bd2) // -2.2 
	println(bd1 * bd2) // 4.08 
	println(bd1 / bd2) // 0.4
  val bi1 = BigInteger("12") 
  val bi2 = BigInteger("34"). 
  println(bi1 + bi2) // 46. 
  println(bi1 - bi2) // -22. 
  println(bi1 * bi2) // 408. 
  println(bi1 / bi2) // 0
}
```

Kotlin/JVM 이외의 플랫폼에서는 무제한 크기와 정밀도로 숫자를 표현하려면 외부 라이브러리가 필요합니다.

### 8. Booleans

```kotlin
fun main() {
	val b1: Boolean = true
	println(b1) // true
	val b2: Boolean = false
	println(b2) // false
}
```

### 9. **Equality**

```kotlin
fun main() {
	println(10 == 10) // true 
	println(10 == 11) // false 
	println(10 != 10) // false 
	println(10 != 11) // true
}

fun main() {
	println(10 > 10) // false 
	println(10 > 11) // false 
	println(11 > 10) // true
	
  println(10 < 10) // false
  println(10 < 11) // true
  println(11 < 10) // false
  
  println(10 >= 10) // true
  println(10 >= 11) // false
  println(11 >= 10) // true
  
  println(10 <= 10) // true
  println(10 <= 11) // true
  println(11 <= 10) // false
}
```

### 10. **Boolean operations**

```kotlin
fun main() {
	println(true && true) // true 
	println(true && false) // false 
	println(false && true) // false 
	println(false && false) // false
	println(true || true) // true 
	println(true || false) // true 
	println(false || true) // true 
	println(false || false) // false
	println(!true) // false
	println(!false) // true
} 
```

Kotlin은 Boolean(또는 다른 어떤 유형)으로의 자동 변환을 전혀 지원하지 않으므로, 논리 연산자는 Boolean 유형의 객체에만 사용해야 합니다.

### 11. Characters

단일 문자를 표현하려면 Char 타입을 사용합니다. 아포스트로피를 사용하여 문자를 지정합니다.

```kotlin
fun main() {
	println('A') // A
	println('Z') // Z
}
```

각 문자는 유니코드 숫자로 표현됩니다. 문자의 유니코드를 확인하려면 code 속성을 사용하세요.

```kotlin
fun main() {
	println('A'.code) // 65
}
```

코틀린은 유니코드 문자를 지원합니다. 코드로 유니코드 문자를 표현하려면 Java와 마찬가지로 \u로 시작하고, 그다음 16진수 형식을 사용해야 합니다.

```kotlin
fun main() {
	println('\u00A3') // £
}
```

### 12. Strings

```kotlin
fun main() {
	val text1 = "ABC"
	println(text1) // ABC
	val text2 = """DEF"""
	println(text2) // DEF
}
```

작은따옴표로 묶인 문자열은 한 줄로 작성해야 합니다. 줄바꿈 문자를 정의하려면 특수 문자 \n을 사용해야 합니다. 문자열에서 백슬래시를 표현해야 하는 경우는 이것뿐만이 아닙니다.

```kotlin
// escape sequence and meaning

\t // tab
\b // Backspace
\r // Carriage return
\f // Form feed
\n // NewLine
\' // Single quotation mark
\" // Quotation mark
\\ // Backslash
\$ // Dollar
```

3중 따옴표로 묶인 문자열은 여러 줄로 구성될 수 있습니다. 이러한 문자열에서는 특수 문자를 직접 사용할 수 있으며 백슬래시로 시작하는 형식은 작동하지 않습니다.

```kotlin
fun main() {
	val text1 = "Let\'s say:\n\"Hooray\""
	println(text1)
	// Let's say:
	// "Hooray"
	
	val text2 = """Let\'s say:\n\"Hooray\""""
	println(text2)
	// Let\'s say:\n\"Hooray\"
	
	val text3 = """Let's say:
	"Hooray""""
	println(text3)
	// Lets's say:
	// "Hooray"
}
```

3중 따옴표 문자열을 더 잘 포맷하기 위해 trimIndent 함수를 사용합니다. 이 함수는 각 줄에 대해 일정한 개수의 공백을 무시합니다.

```kotlin
fun main() {
	val text = """
	Let's say:
	"Hooray"
	""".trimIndent()
	println(text)
	// Let' say:
	// "Hooray"
	
	val description = """
	A
	B
			C
	""".trimIndent()
	println(description)
	// A
	// B
	// 		C
}
```

문자열 리터럴에는 템플릿 표현식이 포함될 수 있습니다. 템플릿 표현식은 평가되어 결과가 문자열로 연결되는 코드 조각입니다. 템플릿 표현식은 달러 기호로 시작하며 변수 이름(예: "text is 𝑡 𝑒 𝑥 𝑡 ") 또는 변수 이름으로 구성됩니다. 𝑜 𝑟 𝑎 𝑛 𝑒 𝑥 𝑝 𝑟 𝑒 𝑠 𝑠 𝑖 𝑜 𝑛 𝑖 𝑠 𝑐 𝑢 𝑟 𝑙 𝑦 𝑏 𝑟 𝑎 𝑐 𝑒 𝑠 ( 𝑙 𝑖 𝑘 𝑒 " 1 + 2 = {1 + 2}").

```kotlin
fun main() {
	val name = "Cookie"
	val surname = "DePies"
	val age = 6
	
	val fullName = "$name $surname ($age)"
	println(fullName) // Cookie DePies (6)
	
	val fullNameUpper = 
	"${name.uppercase()} ${surname.uppercase()} ($age)"
	println(fullNameUpper) // COOKIE DEPIES (6)
	
	val description = """
	Name: $name
	Surname: $surname
	Age: $age
	""".trimIndent()
	println(description)
	// Name: Cookie
	// Surname: DePies
	// Age: 6
}
```

삼중 따옴표 문자열 내부에 특수 문자를 사용해야 하는 경우 가장 쉬운 방법은 일반 문자열로 지정하고 템플릿 구문을 사용하여 포함하는 것입니다.

```kotlin
fun main() {
	val text1 = """ABC\nDEF"""
	println(text1) // ABC\nDEF
	val text2 = """ABC${"\n"}DEF"""
	println(text2)
	// ABC
	// DEF
}
```

Kotlin 문자열에서는 유니코드를 사용하므로 \u로 시작하는 숫자를 사용하여 유니코드 문자를 정의한 다음 16진수 구문으로 유니코드 문자 코드를 지정할 수도 있습니다.

😊

```kotlin
fun main() {
		println("😊") // 😊
		println("\uD83D\uDC4B") //
		println("\uD83C\uDDF5\uD83C\uDDF1") //
}
```

+ 연산자를 사용하면 두 문자열을 연결하여 두 개의 다른 문자열을 조합한 새로운 문자열을 만들 수 있습니다. 또한, 이 연산자를 사용하여 문자열을 다른 객체와 연결할 수 있으며, 이 객체는 문자열로 변환됩니다. 결과는 항상 문자열입니다.

```kotlin
fun main() {
	val text1 = "ABC"
	val text2 = "DEF"
	println(text1 + text2) // ABCDEF
	println(text1 + 123) // ABC123
	println(text1 + true) // ABCtrue
}
```

### 13. Summary

이 장에서는 Kotlin의 기본 타입과 이를 생성하는 데 사용하는 리터럴에 대해 알아보았습니다. Int, Long, Double, Float, Short, Byte 타입으로 표현되는 숫자는 타입 사용자 정의를 위한 접미사를 포함한 단순 숫자 값으로 생성됩니다. 음수나 소수 부분을 정의할 수 있습니다. 또한, 더 나은 숫자 서식을 위해 밑줄을 사용할 수도 있습니다. 부울 값인 true와 false는 Boolean 타입으로 표현됩니다. 문자는 Char 타입으로 표현됩니다. 문자 값은 작은따옴표를 사용하여 정의합니다. 텍스트를 표현하는 문자열은 String 타입으로 표현됩니다. 각 문자열은 일련의 문자로 구성됩니다. 문자열은 큰따옴표 안에 정의합니다. 이제 Kotlin을 사용하기 위한 기초를 다졌습니다. 이제 코드의 동작을 결정하는 더 복잡한 제어 구조로 넘어가 보겠습니다.

1. 기본 타입과 리터럴Kotlin에서는 숫자, 불리언, 문자, 문자열과 같은 기본 값을 표현하기 위해 기본 타입과 리터럴을 제공합니다.숫자: Int, Long, Double, Float, Short, Byte불리언: Boolean (true/false)문자: Char ('a', 'b' 등)문자열: String ("test" 등)리터럴은 특정 값을 나타내기 위한 기본 표기법입니다.예: 123 (정수), 3.14 (실수), "hello" (문자열)
2. 숫자 타입정수형: Int, Long, Byte, Short접미사 L 사용 시 Long 타입 (42L)Byte와 Short는 명시적 타입 지정 필요 (val b: Byte = 123)실수형: Float, Double접미사 F 사용 시 Float 타입 (42F)기본 실수형은 Double (3.14)
3. 명시적 변환Kotlin은 암시적 타입 변환을 지원하지 않음.타입을 변환하려면 .toInt(), .toLong() 등의 변환 함수를 사용해야 함.
4. 숫자 리터럴의 형식긴 숫자에서 _ 사용 가능 (가독성 향상)다른 진법 지원:16진수: 0x로 시작 (0xA4D6FEFE)2진수: 0b로 시작 (0b010101)
5. 수학 연산기본 연산: +, -, *, /, % (나머지) 복합 대입 연산: +=, -=, *=, /=, %= 증감 연산: ++, --
6. 비트 연산Kotlin에서는 비트 연산 메서드를 지원:and, or, xor, shl(왼쪽 쉬프트), shr(오른쪽 쉬프트), ushr(부호 없는 오른쪽 쉬프트)
7. 정밀 계산: BigDecimal과 BigIntegerKotlin 기본 숫자 타입은 크기와 정밀도에 제한이 있음.정밀한 계산이 필요할 때는 BigDecimal(소수 포함) 또는 BigInteger(정수) 사용.변환 함수: .toBigDecimal(), .toBigInteger()
8. 결론Kotlin에서는 모든 기본 타입이 객체로 간주되며, 메서드와 제네릭에서 사용 가능.타입 변환, 연산, 정밀 계산 기능을 활용해 다양한 숫자 관련 작업을 수행할 수 있음.
